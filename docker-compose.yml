version: '3.7'
services:
  postgres:
    image: postgres
    container_name: postgres_db
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: tpweb
      POSTGRES_USER: tpweb
      POSTGRES_PASSWORD: tpweb
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data: